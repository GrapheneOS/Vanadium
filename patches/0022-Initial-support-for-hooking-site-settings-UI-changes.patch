From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: quh4gko8 <88831734+quh4gko8@users.noreply.github.com>
Date: Tue, 7 Apr 2026 09:10:53 +0000
Subject: [PATCH] Initial support for hooking site settings UI changes

---
 .../ContentSettingsResources.java             |  83 ++++++++++++
 .../site_settings/SingleCategorySettings.java |  35 +++++
 .../SingleCategorySettingsHooks.java          |  61 +++++++++
 .../site_settings/SingleWebsiteSettings.java  |  18 +++
 .../SingleWebsiteSettingsHooks.java           |  60 +++++++++
 .../site_settings/SiteSettings.java           |  13 ++
 .../site_settings/SiteSettingsCategory.java   |  60 ++++++++-
 .../SiteSettingsCategoryHooks.java            |  48 +++++++
 .../SiteSettingsHooksRegistry.java            | 125 ++++++++++++++++++
 .../SiteSettingsResourcesHooks.java           |  75 +++++++++++
 .../SiteSettingsScreenHooks.java              | 111 ++++++++++++++++
 .../site_settings/SiteSettingsUtil.java       |   4 +-
 .../browser_ui/site_settings/Website.java     |  36 +++++
 .../WebsitePermissionsFetcher.java            |  19 +++
 .../WebsitePermissionsFetcherHooks.java       |  55 ++++++++
 ...site_settings_android_java_ext_sources.gni |   7 +
 16 files changed, 806 insertions(+), 4 deletions(-)
 create mode 100644 components/browser_ui/site_settings/android/java/src/org/chromium/components/browser_ui/site_settings/SingleCategorySettingsHooks.java
 create mode 100644 components/browser_ui/site_settings/android/java/src/org/chromium/components/browser_ui/site_settings/SingleWebsiteSettingsHooks.java
 create mode 100644 components/browser_ui/site_settings/android/java/src/org/chromium/components/browser_ui/site_settings/SiteSettingsCategoryHooks.java
 create mode 100644 components/browser_ui/site_settings/android/java/src/org/chromium/components/browser_ui/site_settings/SiteSettingsHooksRegistry.java
 create mode 100644 components/browser_ui/site_settings/android/java/src/org/chromium/components/browser_ui/site_settings/SiteSettingsResourcesHooks.java
 create mode 100644 components/browser_ui/site_settings/android/java/src/org/chromium/components/browser_ui/site_settings/SiteSettingsScreenHooks.java
 create mode 100644 components/browser_ui/site_settings/android/java/src/org/chromium/components/browser_ui/site_settings/WebsitePermissionsFetcherHooks.java

diff --git a/components/browser_ui/site_settings/android/java/src/org/chromium/components/browser_ui/site_settings/ContentSettingsResources.java b/components/browser_ui/site_settings/android/java/src/org/chromium/components/browser_ui/site_settings/ContentSettingsResources.java
index 9e2f2fed5cf43..7558180af3f65 100644
--- a/components/browser_ui/site_settings/android/java/src/org/chromium/components/browser_ui/site_settings/ContentSettingsResources.java
+++ b/components/browser_ui/site_settings/android/java/src/org/chromium/components/browser_ui/site_settings/ContentSettingsResources.java
@@ -144,6 +144,89 @@ public class ContentSettingsResources {
 
     /** Returns the ResourceItem for a ContentSettingsType. */
     private static ResourceItem getResourceItem(int contentType) {
+        SiteSettingsResourcesHooks hooks =
+                SiteSettingsHooksRegistry.getResourcesHooks(contentType);
+        if (hooks.isDefaultInstance()) {
+            return getResourceItemInner(contentType);
+        }
+
+        int icon = hooks.getIconRes();
+        int iconBlocked = hooks.getIconBlockedRes();
+        int title = hooks.getTitleRes();
+        int defaultEnabledValue = hooks.getDefaultEnabledValue();
+        int defaultDisabledValue = hooks.getDefaultDisabledValue();
+        int enabledSummary = hooks.getEnabledSummaryRes();
+        int disabledSummary = hooks.getDisabledSummaryRes();
+        int summaryOverrideForScreenReader = hooks.getSummaryOverrideForScreenReaderRes();
+        int enabledPrimaryText = hooks.getEnabledPrimaryTextRes();
+        int disabledPrimaryText = hooks.getDisabledPrimaryTextRes();
+        int disabledDescriptionText = hooks.getDisabledDescriptionTextRes();
+        final int noResOverride = SiteSettingsResourcesHooks.NO_RES_OVERRIDE;
+        if (icon == noResOverride
+                || iconBlocked == noResOverride
+                || title == noResOverride
+                || defaultEnabledValue == ContentSetting.DEFAULT
+                || defaultDisabledValue == ContentSetting.DEFAULT
+                || enabledSummary == noResOverride
+                || disabledSummary == noResOverride
+                || summaryOverrideForScreenReader == noResOverride
+                || enabledPrimaryText == noResOverride
+                || disabledPrimaryText == noResOverride) {
+            ResourceItem origResItem = getResourceItemInner(contentType);
+            if (icon == noResOverride) {
+                icon = origResItem.getIcon();
+            }
+            if (title == noResOverride) {
+                title = origResItem.getTitle();
+            }
+            if (defaultEnabledValue == ContentSetting.DEFAULT) {
+                defaultEnabledValue = origResItem.getDefaultEnabledValue();
+            }
+            if (defaultDisabledValue == ContentSetting.DEFAULT) {
+                defaultDisabledValue = origResItem.getDefaultDisabledValue();
+            }
+            if (enabledSummary == noResOverride) {
+                enabledSummary = origResItem.getEnabledSummary();
+            }
+            if (disabledSummary == noResOverride) {
+                disabledSummary = origResItem.getDisabledSummary();
+            }
+            if (summaryOverrideForScreenReader == noResOverride) {
+                summaryOverrideForScreenReader = origResItem.getSummaryOverrideForScreenReader();
+            }
+            if (iconBlocked == noResOverride) {
+                iconBlocked = origResItem.getIconBlocked();
+            }
+            if (enabledPrimaryText == noResOverride) {
+                enabledPrimaryText = origResItem.getEnabledPrimaryText();
+            }
+            if (disabledPrimaryText == noResOverride) {
+                disabledPrimaryText = origResItem.getDisabledPrimaryText();
+            }
+            if (disabledDescriptionText == noResOverride) {
+                disabledDescriptionText = origResItem.getDisabledDescriptionText();
+            }
+        }
+
+        ResourceItem resourceItem = new ResourceItem(
+                icon,
+                title,
+                defaultEnabledValue,
+                defaultDisabledValue,
+                enabledSummary,
+                disabledSummary,
+                summaryOverrideForScreenReader,
+                iconBlocked,
+                enabledPrimaryText,
+                disabledPrimaryText);
+        if (disabledDescriptionText == 0 || disabledDescriptionText == noResOverride) {
+            return resourceItem;
+        }
+
+        return resourceItem.setDisabledDescriptionText(disabledDescriptionText);
+    }
+
+    private static ResourceItem getResourceItemInner(int contentType) {
         switch (contentType) {
             case ContentSettingsType.ADS:
                 return new ResourceItem(
diff --git a/components/browser_ui/site_settings/android/java/src/org/chromium/components/browser_ui/site_settings/SingleCategorySettings.java b/components/browser_ui/site_settings/android/java/src/org/chromium/components/browser_ui/site_settings/SingleCategorySettings.java
index d640b69afaff7..b7027b24366d6 100644
--- a/components/browser_ui/site_settings/android/java/src/org/chromium/components/browser_ui/site_settings/SingleCategorySettings.java
+++ b/components/browser_ui/site_settings/android/java/src/org/chromium/components/browser_ui/site_settings/SingleCategorySettings.java
@@ -484,6 +484,22 @@ public class SingleCategorySettings extends BaseSiteSettingsFragment
             mGlobalToggleLayout = GlobalToggleLayout.BINARY_RADIO_BUTTON;
         }
 
+        SingleCategorySettingsHooks hooks =
+                SiteSettingsHooksRegistry.getSingleCategorySettingsHooks(mCategory.getType());
+        if (!hooks.isDefaultInstance()) {
+            int globalToggleLayoutOverrides = hooks.getGlobalToggleLayoutOverride();
+            switch (globalToggleLayoutOverrides) {
+                case SingleCategorySettingsHooks.BINARY_RADIO_BUTTON -> {
+                    mGlobalToggleLayout = GlobalToggleLayout.BINARY_RADIO_BUTTON;
+                }
+                case SingleCategorySettingsHooks.TRI_STATE_TOGGLE -> {
+                    mGlobalToggleLayout = GlobalToggleLayout.TRI_STATE_TOGGLE;
+                }
+                default -> {
+                }
+            }
+        }
+
         ViewGroup view = (ViewGroup) super.onCreateView(inflater, container, savedInstanceState);
 
         mListView = getListView();
@@ -793,6 +809,15 @@ public class SingleCategorySettings extends BaseSiteSettingsFragment
     }
 
     private int getAddExceptionDialogMessageResourceId() {
+        SingleCategorySettingsHooks hooks =
+                SiteSettingsHooksRegistry.getSingleCategorySettingsHooks(mCategory.getType());
+        if (!hooks.isDefaultInstance()) {
+            int resId = hooks.getAddExceptionDialogMessageResourceId(isCategoryEnabled());
+            if (resId != SiteSettingsResourcesHooks.NO_RES_OVERRIDE) {
+                return resId;
+            }
+        }
+
         switch (mCategory.getType()) {
             case SiteSettingsCategory.Type.AUTOMATIC_DOWNLOADS:
                 return isCategoryEnabled()
@@ -952,6 +977,16 @@ public class SingleCategorySettings extends BaseSiteSettingsFragment
                 break;
         }
 
+        SingleCategorySettingsHooks hooks =
+                SiteSettingsHooksRegistry.getSingleCategorySettingsHooks(mCategory.getType());
+        if (!hooks.isDefaultInstance()) {
+            final Boolean shouldAddExceptionButtonOverride =
+                    hooks.shouldAddExceptionButton(isCategoryEnabled());
+            if (shouldAddExceptionButtonOverride != null) {
+                shouldAddExceptionButton = shouldAddExceptionButtonOverride;
+            }
+        }
+
         int exceptionDialogMessageResourceId = getAddExceptionDialogMessageResourceId();
         assert shouldAddExceptionButton == (exceptionDialogMessageResourceId != 0);
 
diff --git a/components/browser_ui/site_settings/android/java/src/org/chromium/components/browser_ui/site_settings/SingleCategorySettingsHooks.java b/components/browser_ui/site_settings/android/java/src/org/chromium/components/browser_ui/site_settings/SingleCategorySettingsHooks.java
new file mode 100644
index 0000000000000..e681344f6243a
--- /dev/null
+++ b/components/browser_ui/site_settings/android/java/src/org/chromium/components/browser_ui/site_settings/SingleCategorySettingsHooks.java
@@ -0,0 +1,61 @@
+package org.chromium.components.browser_ui.site_settings;
+
+import androidx.annotation.IntDef;
+import androidx.preference.Preference;
+
+import org.chromium.build.annotations.AlwaysInline;
+import org.chromium.build.annotations.NonNull;
+import org.chromium.build.annotations.NullMarked;
+import org.chromium.build.annotations.Nullable;
+import org.chromium.components.browser_ui.settings.ChromeBasePreference;
+import org.chromium.components.content_settings.ContentSettingsType;
+
+@NullMarked
+public class SingleCategorySettingsHooks {
+
+    private static final class LazyHolder {
+        @NonNull
+        private static final SingleCategorySettingsHooks DEF_INSTANCE =
+                new SingleCategorySettingsHooks();
+    }
+
+    protected SingleCategorySettingsHooks() {
+    }
+
+    protected final boolean isDefaultInstance() {
+        return this == getDefaultInstance();
+    }
+
+    @NonNull
+    protected static final SingleCategorySettingsHooks getDefaultInstance() {
+        return LazyHolder.DEF_INSTANCE;
+    }
+
+    static final int NONE = -1;
+    static final int BINARY_RADIO_BUTTON = 0;
+    static final int TRI_STATE_TOGGLE = 1;
+    static final int OTHER_LAYOUT_TOGGLE = 2;
+
+    @IntDef({
+        NONE,
+        BINARY_RADIO_BUTTON,
+        TRI_STATE_TOGGLE,
+        OTHER_LAYOUT_TOGGLE,
+    })
+    @interface GlobalToggleLayoutOverride {
+    }
+
+    @GlobalToggleLayoutOverride
+    protected int getGlobalToggleLayoutOverride() {
+        return NONE;
+    }
+
+    @Nullable
+    protected Boolean shouldAddExceptionButton(boolean isCategoryEnabled) {
+        return null;
+    }
+
+    protected int getAddExceptionDialogMessageResourceId(boolean isCategoryEnabled) {
+        return SiteSettingsResourcesHooks.NO_RES_OVERRIDE;
+    }
+}
diff --git a/components/browser_ui/site_settings/android/java/src/org/chromium/components/browser_ui/site_settings/SingleWebsiteSettings.java b/components/browser_ui/site_settings/android/java/src/org/chromium/components/browser_ui/site_settings/SingleWebsiteSettings.java
index 0fd4b9c345d93..76a8468578198 100644
--- a/components/browser_ui/site_settings/android/java/src/org/chromium/components/browser_ui/site_settings/SingleWebsiteSettings.java
+++ b/components/browser_ui/site_settings/android/java/src/org/chromium/components/browser_ui/site_settings/SingleWebsiteSettings.java
@@ -143,6 +143,15 @@ public class SingleWebsiteSettings extends BaseSiteSettingsFragment
      */
     @VisibleForTesting
     public static @Nullable String getPreferenceKey(@ContentSettingsType.EnumType int type) {
+        SingleWebsiteSettingsHooks hooks =
+                SiteSettingsHooksRegistry.getSingleWebsiteSettingsHooks(type);
+        if (!hooks.isDefaultInstance()) {
+            String prefKey = hooks.getPreferenceKey();
+            if (prefKey != null && !"".equals(prefKey)) {
+                return prefKey;
+            }
+        }
+
         switch (type) {
             case ContentSettingsType.ADS:
                 return "ads_permission_list";
@@ -659,6 +668,15 @@ public class SingleWebsiteSettings extends BaseSiteSettingsFragment
         for (@ContentSettingsType.EnumType int type : SiteSettingsUtil.SETTINGS_ORDER) {
             Preference preference = getPermissionPreference(type);
             preference.setKey(getPreferenceKey(type));
+            SingleWebsiteSettingsHooks hooks =
+                    SiteSettingsHooksRegistry.getSingleWebsiteSettingsHooks(type);
+            if (!hooks.isDefaultInstance()) {
+                setupContentSettingsPreference(
+                        preference,
+                        hooks.getContentSettingForSetup(getBrowserContextHandle(), mSite, type),
+                        mSite.isEmbargoed(type),
+                        isOneTime(type));
+            }
 
             if (type == ContentSettingsType.ADS) {
                 setUpAdsPreference(preference);
diff --git a/components/browser_ui/site_settings/android/java/src/org/chromium/components/browser_ui/site_settings/SingleWebsiteSettingsHooks.java b/components/browser_ui/site_settings/android/java/src/org/chromium/components/browser_ui/site_settings/SingleWebsiteSettingsHooks.java
new file mode 100644
index 0000000000000..5737bb2acebdd
--- /dev/null
+++ b/components/browser_ui/site_settings/android/java/src/org/chromium/components/browser_ui/site_settings/SingleWebsiteSettingsHooks.java
@@ -0,0 +1,60 @@
+package org.chromium.components.browser_ui.site_settings;
+
+import androidx.preference.Preference;
+
+import org.chromium.build.annotations.AlwaysInline;
+import org.chromium.build.annotations.NonNull;
+import org.chromium.build.annotations.NullMarked;
+import org.chromium.build.annotations.Nullable;
+import org.chromium.components.browser_ui.settings.ChromeBasePreference;
+import org.chromium.components.content_settings.ContentSettingsType;
+import org.chromium.content_public.browser.BrowserContextHandle;
+
+@NullMarked
+public class SingleWebsiteSettingsHooks {
+
+    private static final class LazyHolder {
+        @NonNull
+        private static final SingleWebsiteSettingsHooks DEF_INSTANCE =
+                new SingleWebsiteSettingsHooks();
+    }
+
+    protected SingleWebsiteSettingsHooks() {
+    }
+
+    protected final boolean isDefaultInstance() {
+        return this == getDefaultInstance();
+    }
+
+    @NonNull
+    protected static final SingleWebsiteSettingsHooks getDefaultInstance() {
+        return LazyHolder.DEF_INSTANCE;
+    }
+
+    @NonNull
+    protected String getPreferenceKey() {
+        return "";
+    }
+
+    @Nullable
+    protected Integer getContentSettingForSetup(
+            @NonNull BrowserContextHandle browserContextHandle,
+            @NonNull Website website,
+            @ContentSettingsType.EnumType int contentType) {
+        return null;
+    }
+
+    /**
+     * Has to be of following types for now:
+     * org.chromium.components.browser_ui.settings.ChromeImageViewPreference
+     * org.chromium.components.browser_ui.settings.ChromeSwitchPreference
+     * currently disabled until further notice
+    @Nullable
+    protected Preference getOverridenPreference(
+            @NonNull SiteSettings siteSettingsFragment,
+            @NonNull SiteSettingsDelegate siteSettingsDelegate,
+            @ContentSettingsType.EnumType int contentType) {
+        return null;
+    }
+     */
+}
diff --git a/components/browser_ui/site_settings/android/java/src/org/chromium/components/browser_ui/site_settings/SiteSettings.java b/components/browser_ui/site_settings/android/java/src/org/chromium/components/browser_ui/site_settings/SiteSettings.java
index fc9f116ff6544..6843ee392312a 100644
--- a/components/browser_ui/site_settings/android/java/src/org/chromium/components/browser_ui/site_settings/SiteSettings.java
+++ b/components/browser_ui/site_settings/android/java/src/org/chromium/components/browser_ui/site_settings/SiteSettings.java
@@ -92,6 +92,13 @@ public class SiteSettings extends BaseSiteSettingsFragment
         for (@SiteSettingsCategory.Type int type = 0;
                 type < SiteSettingsCategory.Type.NUM_ENTRIES;
                 type++) {
+            SiteSettingsScreenHooks hooks =
+                    SiteSettingsHooksRegistry.getSiteSettingsScreenHooks(type);
+            if (!hooks.isDefaultInstance()) {
+                hooks.configureSiteSettingsPreference(
+                        this, getSiteSettingsDelegate(), type,
+                        findPreference(type));
+            }
             if (!getSiteSettingsDelegate().isCategoryVisible(type)) {
                 Preference pref = findPreference(type);
                 getPreferenceScreen().removePreference(pref);
@@ -202,6 +209,12 @@ public class SiteSettings extends BaseSiteSettingsFragment
                         SettingsUtils.getTintedIcon(
                                 getContext(), ContentSettingsResources.getIcon(contentType)));
             }
+
+            SiteSettingsScreenHooks hooks =
+                    SiteSettingsHooksRegistry.getSiteSettingsScreenHooks(prefCategory);
+            if (!hooks.isDefaultInstance()) {
+                hooks.updateSiteSettingsPreference(this, getSiteSettingsDelegate(), p);
+            }
         }
 
         // For AllSiteSettings options.
diff --git a/components/browser_ui/site_settings/android/java/src/org/chromium/components/browser_ui/site_settings/SiteSettingsCategory.java b/components/browser_ui/site_settings/android/java/src/org/chromium/components/browser_ui/site_settings/SiteSettingsCategory.java
index d335c6e2ea9ed..b51d43d085ab4 100644
--- a/components/browser_ui/site_settings/android/java/src/org/chromium/components/browser_ui/site_settings/SiteSettingsCategory.java
+++ b/components/browser_ui/site_settings/android/java/src/org/chromium/components/browser_ui/site_settings/SiteSettingsCategory.java
@@ -83,7 +83,13 @@ public class SiteSettingsCategory {
         Type.AUTO_PICTURE_IN_PICTURE,
         Type.LOCAL_NETWORK,
         Type.LOOPBACK_NETWORK,
-        Type.NUM_ENTRIES
+        // Three lines of placeholder comment to minimize cascading conflicts.
+        // Three lines of placeholder comment to minimize cascading conflicts.
+        // Three lines of placeholder comment to minimize cascading conflicts.
+        Type.NUM_ENTRIES,
+        // Three lines of placeholder comment to minimize cascading conflicts.
+        // Three lines of placeholder comment to minimize cascading conflicts.
+        // Three lines of placeholder comment to minimize cascading conflicts.
     })
     @Retention(RetentionPolicy.SOURCE)
     public @interface Type {
@@ -130,7 +136,28 @@ public class SiteSettingsCategory {
         int LOOPBACK_NETWORK = 38;
 
         /** Number of handled categories used for calculating array sizes. */
-        int NUM_ENTRIES = 39;
+        int NUM_ENTRIES_CHROMIUM = 39;
+
+        // Three lines of placeholder comment to minimize cascading conflicts.
+        // Three lines of placeholder comment to minimize cascading conflicts.
+        // Three lines of placeholder comment to minimize cascading conflicts.
+        int NUM_ENTRIES = NUM_ENTRIES_CHROMIUM + ExtType.NUM_ENTRIES;
+        // Three lines of placeholder comment to minimize cascading conflicts.
+        // Three lines of placeholder comment to minimize cascading conflicts.
+        // Three lines of placeholder comment to minimize cascading conflicts.
+    }
+
+    @IntDef({
+        ExtType.NUM_ENTRIES,
+    })
+    @interface ExtType {
+        // Three lines of placeholder comment to minimize cascading conflicts.
+        // Three lines of placeholder comment to minimize cascading conflicts.
+        // Three lines of placeholder comment to minimize cascading conflicts.
+        int NUM_ENTRIES = 0;
+        // Three lines of placeholder comment to minimize cascading conflicts.
+        // Three lines of placeholder comment to minimize cascading conflicts.
+        // Three lines of placeholder comment to minimize cascading conflicts.
     }
 
     private final BrowserContextHandle mBrowserContextHandle;
@@ -167,6 +194,16 @@ public class SiteSettingsCategory {
     /** Construct a SiteSettingsCategory from a type. */
     public static SiteSettingsCategory createFromType(
             BrowserContextHandle browserContextHandle, @Type int type) {
+        SiteSettingsCategoryHooks hooks =
+                SiteSettingsHooksRegistry.getCategoryHooks(type);
+        if (!hooks.isDefaultInstance()) {
+            SiteSettingsCategory category =
+                    hooks.getSiteSettingsCategory(browserContextHandle);
+            if (category != null) {
+                return category;
+            }
+        }
+
         if (type == Type.DEVICE_LOCATION) {
             return new LocationCategory(browserContextHandle, /* forPreciseLocation= */ true);
         }
@@ -218,6 +255,16 @@ public class SiteSettingsCategory {
 
     /** Convert Type into {@link ContentSettingsType}. */
     public static @ContentSettingsType.EnumType int contentSettingsType(@Type int type) {
+        SiteSettingsCategoryHooks hooks =
+                SiteSettingsHooksRegistry.getCategoryHooks(type);
+        if (!hooks.isDefaultInstance()) {
+            @ContentSettingsType.EnumType
+            int contentSettingsType = hooks.getContentSettingsType();
+            if (contentSettingsType != ContentSettingsType.DEFAULT) {
+                return contentSettingsType;
+            }
+        }
+
         // This switch statement is ordered by types alphabetically.
         switch (type) {
             case Type.ADS:
@@ -319,6 +366,15 @@ public class SiteSettingsCategory {
 
     /** Convert Type into preference String */
     public static String preferenceKey(@Type int type) {
+        SiteSettingsCategoryHooks hooks =
+                SiteSettingsHooksRegistry.getCategoryHooks(type);
+        if (!hooks.isDefaultInstance()) {
+            String prefKey = hooks.getPreferenceKey();
+            if (prefKey != null && !"".equals(prefKey)) {
+                return prefKey;
+            }
+        }
+
         // This switch statement is ordered by types alphabetically.
         switch (type) {
             case Type.ADS:
diff --git a/components/browser_ui/site_settings/android/java/src/org/chromium/components/browser_ui/site_settings/SiteSettingsCategoryHooks.java b/components/browser_ui/site_settings/android/java/src/org/chromium/components/browser_ui/site_settings/SiteSettingsCategoryHooks.java
new file mode 100644
index 0000000000000..d4f611ebaeefa
--- /dev/null
+++ b/components/browser_ui/site_settings/android/java/src/org/chromium/components/browser_ui/site_settings/SiteSettingsCategoryHooks.java
@@ -0,0 +1,48 @@
+package org.chromium.components.browser_ui.site_settings;
+
+import androidx.annotation.IntDef;
+
+import org.chromium.build.annotations.AlwaysInline;
+import org.chromium.build.annotations.NonNull;
+import org.chromium.build.annotations.NullMarked;
+import org.chromium.build.annotations.Nullable;
+import org.chromium.components.content_settings.ContentSettingsType;
+import org.chromium.content_public.browser.BrowserContextHandle;
+
+@NullMarked
+public class SiteSettingsCategoryHooks {
+
+    private static final class LazyHolder {
+        @NonNull
+        private static final SiteSettingsCategoryHooks DEF_INSTANCE =
+                new SiteSettingsCategoryHooks();
+    }
+
+    protected SiteSettingsCategoryHooks() {
+    }
+
+    protected final boolean isDefaultInstance() {
+        return this == getDefaultInstance();
+    }
+
+    @NonNull
+    protected static final SiteSettingsCategoryHooks getDefaultInstance() {
+        return LazyHolder.DEF_INSTANCE;
+    }
+
+    @NonNull
+    protected String getPreferenceKey() {
+        return "";
+    }
+
+    @ContentSettingsType.EnumType
+    protected int getContentSettingsType() {
+        return ContentSettingsType.DEFAULT;
+    }
+
+    @Nullable
+    protected SiteSettingsCategory getSiteSettingsCategory(
+            @NonNull BrowserContextHandle bch) {
+        return null;
+    }
+}
diff --git a/components/browser_ui/site_settings/android/java/src/org/chromium/components/browser_ui/site_settings/SiteSettingsHooksRegistry.java b/components/browser_ui/site_settings/android/java/src/org/chromium/components/browser_ui/site_settings/SiteSettingsHooksRegistry.java
new file mode 100644
index 0000000000000..e35725146caeb
--- /dev/null
+++ b/components/browser_ui/site_settings/android/java/src/org/chromium/components/browser_ui/site_settings/SiteSettingsHooksRegistry.java
@@ -0,0 +1,125 @@
+package org.chromium.components.browser_ui.site_settings;
+
+import org.chromium.build.annotations.NonNull;
+import org.chromium.build.annotations.NullMarked;
+import org.chromium.components.content_settings.ContentSettingsType;
+
+import java.util.LinkedHashMap;
+
+@NullMarked
+public final class SiteSettingsHooksRegistry {
+
+    @SiteSettingsCategory.ExtType
+    public static int getExtTypeFromCategoryType(
+            @SiteSettingsCategory.Type int categoryType) {
+        return switch (categoryType) {
+            default -> SiteSettingsCategory.ExtType.NUM_ENTRIES;
+        };
+    }
+
+    @NonNull
+    public static SingleCategorySettingsHooks getSingleCategorySettingsHooks(
+            @SiteSettingsCategory.Type int categoryType) {
+        @SiteSettingsCategory.ExtType
+        int extType = getExtTypeFromCategoryType(categoryType);
+        return switch (extType) {
+            default -> SingleCategorySettingsHooks.getDefaultInstance();
+        };
+    }
+
+    @NonNull
+    public static SingleWebsiteSettingsHooks getSingleWebsiteSettingsHooks(
+            @ContentSettingsType.EnumType int contentType) {
+        return switch (contentType) {
+            default -> SingleWebsiteSettingsHooks.getDefaultInstance();
+        };
+    }
+
+    @NonNull
+    public static SiteSettingsCategoryHooks getCategoryHooks(
+            @SiteSettingsCategory.Type int categoryType) {
+        @SiteSettingsCategory.ExtType
+        int extType = getExtTypeFromCategoryType(categoryType);
+        return switch (extType) {
+            default -> SiteSettingsCategoryHooks.getDefaultInstance();
+        };
+    }
+
+    @NonNull
+    public static SiteSettingsResourcesHooks getResourcesHooks(
+            @ContentSettingsType.EnumType int contentType) {
+        return switch (contentType) {
+            default -> SiteSettingsResourcesHooks.getDefaultInstance();
+        };
+    }
+
+    @NonNull
+    public static SiteSettingsScreenHooks getSiteSettingsScreenHooks(
+            @SiteSettingsCategory.Type int categoryType) {
+        @SiteSettingsCategory.ExtType
+        int extType = getExtTypeFromCategoryType(categoryType);
+        return switch (extType) {
+            default -> SiteSettingsScreenHooks.getDefaultInstance();
+        };
+    }
+
+    @NonNull
+    public static WebsitePermissionsFetcherHooks getWebsitePermissionsFetcherHooks(
+            @ContentSettingsType.EnumType int contentType) {
+        return switch (contentType) {
+            default -> WebsitePermissionsFetcherHooks.getDefaultInstance();
+        };
+    }
+
+    @NonNull
+    @ContentSettingsType.EnumType
+    static final int[] getNewSettingsOrder(
+            @NonNull @ContentSettingsType.EnumType int[] existingOrder) {
+        LinkedHashMap<Integer, LinkedHashMap<Integer, Boolean>>
+                prevToNextContentTypeMap = new LinkedHashMap<>();
+        for (int i = SiteSettingsCategory.Type.NUM_ENTRIES_CHROMIUM;
+                i < SiteSettingsCategory.Type.NUM_ENTRIES; ++i) {
+            SiteSettingsScreenHooks hooks = getSiteSettingsScreenHooks(i);
+            if (!hooks.isDefaultInstance()) {
+                int contentSettingsType = SiteSettingsCategory.contentSettingsType(i);
+                if (contentSettingsType == ContentSettingsType.DEFAULT) {
+                    continue;
+                }
+                int prevContentType = hooks.getPreviousContentSettings();
+                LinkedHashMap<Integer, Boolean> nextContentTypes =
+                        prevToNextContentTypeMap.getOrDefault(prevContentType, new LinkedHashMap<>());
+                nextContentTypes.put(contentSettingsType, true);
+                prevToNextContentTypeMap.put(prevContentType, nextContentTypes);
+            }
+        }
+
+        var newOrder = new LinkedHashMap<Integer, Boolean>();
+        for (int contentType: existingOrder) {
+            newOrder.put(contentType, true);
+            LinkedHashMap<Integer, Boolean> nextContentTypes =
+                    prevToNextContentTypeMap.getOrDefault(contentType,
+                            new LinkedHashMap<>());
+            newOrder.putAll(nextContentTypes);
+        }
+
+        int newOrderSize = newOrder.size();
+        @ContentSettingsType.EnumType
+        int[] newOrderArr = new int[newOrder.size()];
+        {
+            int[] index = new int[1];
+            index[0] = 0;
+            newOrder.forEach((contentType, unused) -> {
+                int i = index[0];
+                if (i >= newOrderSize) {
+                    return;
+                }
+                newOrderArr[i] = contentType;
+                i++;
+                index[0] = i;
+            });
+            index[0] = 0;
+        }
+
+        return newOrderArr;
+    }
+}
diff --git a/components/browser_ui/site_settings/android/java/src/org/chromium/components/browser_ui/site_settings/SiteSettingsResourcesHooks.java b/components/browser_ui/site_settings/android/java/src/org/chromium/components/browser_ui/site_settings/SiteSettingsResourcesHooks.java
new file mode 100644
index 0000000000000..50fecca54b3e4
--- /dev/null
+++ b/components/browser_ui/site_settings/android/java/src/org/chromium/components/browser_ui/site_settings/SiteSettingsResourcesHooks.java
@@ -0,0 +1,75 @@
+package org.chromium.components.browser_ui.site_settings;
+
+import org.chromium.build.annotations.NonNull;
+import org.chromium.build.annotations.NullMarked;
+import org.chromium.components.content_settings.ContentSetting;
+
+@NullMarked
+public class SiteSettingsResourcesHooks {
+
+    public static final int NO_RES_OVERRIDE = -1;
+
+    private static final class LazyHolder {
+        @NonNull
+        private static final SiteSettingsResourcesHooks DEF_INSTANCE =
+                new SiteSettingsResourcesHooks();
+    }
+
+    protected SiteSettingsResourcesHooks() {
+    }
+
+    protected final boolean isDefaultInstance() {
+        return this == getDefaultInstance();
+    }
+
+    @NonNull
+    protected static final SiteSettingsResourcesHooks getDefaultInstance() {
+        return LazyHolder.DEF_INSTANCE;
+    }
+
+    public int getIconRes() {
+        return NO_RES_OVERRIDE;
+    }
+
+    public int getIconBlockedRes() {
+        return NO_RES_OVERRIDE;
+    }
+
+    public int getTitleRes() {
+        return NO_RES_OVERRIDE;
+    }
+
+    @ContentSetting
+    public int getDefaultEnabledValue() {
+        return ContentSetting.DEFAULT;
+    }
+
+    @ContentSetting
+    public int getDefaultDisabledValue() {
+        return ContentSetting.DEFAULT;
+    }
+
+    public int getEnabledSummaryRes() {
+        return NO_RES_OVERRIDE;
+    }
+
+    public int getDisabledSummaryRes() {
+        return NO_RES_OVERRIDE;
+    }
+
+    public int getSummaryOverrideForScreenReaderRes() {
+        return NO_RES_OVERRIDE;
+    }
+
+    public int getEnabledPrimaryTextRes() {
+        return NO_RES_OVERRIDE;
+    }
+
+    public int getDisabledPrimaryTextRes() {
+        return NO_RES_OVERRIDE;
+    }
+
+    public int getDisabledDescriptionTextRes() {
+        return NO_RES_OVERRIDE;
+    }
+}
diff --git a/components/browser_ui/site_settings/android/java/src/org/chromium/components/browser_ui/site_settings/SiteSettingsScreenHooks.java b/components/browser_ui/site_settings/android/java/src/org/chromium/components/browser_ui/site_settings/SiteSettingsScreenHooks.java
new file mode 100644
index 0000000000000..b2baf6e628cf9
--- /dev/null
+++ b/components/browser_ui/site_settings/android/java/src/org/chromium/components/browser_ui/site_settings/SiteSettingsScreenHooks.java
@@ -0,0 +1,111 @@
+package org.chromium.components.browser_ui.site_settings;
+
+import android.content.Context;
+
+import androidx.preference.Preference;
+import androidx.preference.PreferenceScreen;
+
+import org.chromium.build.NullUtil;
+import org.chromium.build.annotations.AlwaysInline;
+import org.chromium.build.annotations.NonNull;
+import org.chromium.build.annotations.NullMarked;
+import org.chromium.build.annotations.Nullable;
+import org.chromium.components.browser_ui.settings.ChromeBasePreference;
+import org.chromium.components.content_settings.ContentSettingsType;
+import org.chromium.content_public.browser.BrowserContextHandle;
+
+@NullMarked
+public class SiteSettingsScreenHooks {
+
+    private static final class LazyHolder {
+        @NonNull
+        private static final SiteSettingsScreenHooks DEF_INSTANCE =
+                new SiteSettingsScreenHooks();
+    }
+
+    protected SiteSettingsScreenHooks() {
+    }
+
+    protected final boolean isDefaultInstance() {
+        return this == getDefaultInstance();
+    }
+
+    @NonNull
+    protected static final SiteSettingsScreenHooks getDefaultInstance() {
+        return LazyHolder.DEF_INSTANCE;
+    }
+
+    @ContentSettingsType.EnumType
+    protected int getPreviousContentSettings() {
+        return ContentSettingsType.MAX_VALUE;
+    }
+
+    protected void configureSiteSettingsPreference(
+            @NonNull SiteSettings siteSettingsFragment,
+            @NonNull SiteSettingsDelegate siteSettingsDelegate,
+            @SiteSettingsCategory.Type int categoryType,
+            @Nullable Preference existingPreference) {
+    }
+
+    protected void updateSiteSettingsPreference(
+            @NonNull SiteSettings siteSettingsFragment,
+            @NonNull SiteSettingsDelegate siteSettingsDelegate,
+            @NonNull Preference existingPreference) {
+    }
+
+    protected final ChromeBasePreference defaultCreatePreference(
+            @NonNull SiteSettings siteSettingsFragment,
+            @NonNull String prefKey) {
+        Context prefCtx = siteSettingsFragment.requireContext();
+        ChromeBasePreference pref = new ChromeBasePreference(prefCtx);
+        String fragmentName = "org.chromium.components.browser_ui.site_settings.SingleCategorySettings";
+        pref.setFragment(fragmentName);
+        pref.setKey(prefKey);
+        return pref;
+    }
+
+    protected final void addPreference(
+            @NonNull SiteSettings siteSettingsFragment,
+            @NonNull SiteSettingsDelegate siteSettingsDelegate,
+            @SiteSettingsCategory.Type int categoryType,
+            @NonNull Preference pref) {
+        PreferenceScreen preferenceScreen = siteSettingsFragment.getPreferenceScreen();
+        int prevContentSetting = getPreviousContentSettings();
+        int curContentSetting = SiteSettingsCategory.contentSettingsType(categoryType);
+        WebsitePermissionsFetcher.WebsitePermissionsType permissionsType =
+                WebsitePermissionsFetcher.getPermissionsType(curContentSetting);
+        int prefCount = preferenceScreen.getPreferenceCount();
+        final int orderDefault;
+        if (permissionsType == null) {
+            orderDefault = prefCount - 2;
+        } else if (permissionsType == WebsitePermissionsFetcher.WebsitePermissionsType.PERMISSION_INFO) {
+            Preference permissionGroup = preferenceScreen.findPreference("content_category");
+            orderDefault = permissionGroup.getOrder() - 2;
+        } else {
+            orderDefault = prefCount - 2;
+        }
+        if (prevContentSetting >= ContentSettingsType.MAX_VALUE) {
+            addPreferenceInner(preferenceScreen, pref, orderDefault);
+            return;
+        }
+
+        SiteSettingsCategory prevCategory = SiteSettingsCategory.createFromContentSettingsType(
+                siteSettingsDelegate.getBrowserContextHandle(), prevContentSetting);
+        if (prevCategory == null) {
+            addPreferenceInner(preferenceScreen, pref, orderDefault);
+            return;
+        }
+
+        String prevPrefKey = SiteSettingsCategory.preferenceKey(prevCategory.getType());
+        Preference prevPref = NullUtil.assertNonNull(siteSettingsFragment.findPreference(prevPrefKey));
+        int prevOrder = prevPref.getOrder();
+        addPreferenceInner(preferenceScreen, pref, prevOrder);
+    }
+
+    private final void addPreferenceInner(@NonNull PreferenceScreen preferenceScreen,
+            @NonNull Preference pref, int prevOrder) {
+        pref.setOrder(prevOrder + 1);
+        preferenceScreen.addPreference(pref);
+    }
+}
+
diff --git a/components/browser_ui/site_settings/android/java/src/org/chromium/components/browser_ui/site_settings/SiteSettingsUtil.java b/components/browser_ui/site_settings/android/java/src/org/chromium/components/browser_ui/site_settings/SiteSettingsUtil.java
index c68709a418086..615e27440294e 100644
--- a/components/browser_ui/site_settings/android/java/src/org/chromium/components/browser_ui/site_settings/SiteSettingsUtil.java
+++ b/components/browser_ui/site_settings/android/java/src/org/chromium/components/browser_ui/site_settings/SiteSettingsUtil.java
@@ -24,7 +24,7 @@ import org.chromium.content_public.browser.HostZoomMap;
 public class SiteSettingsUtil {
     // Defining the order for content settings based on http://crbug.com/610358
     @VisibleForTesting(otherwise = VisibleForTesting.PACKAGE_PRIVATE)
-    public static final int[] SETTINGS_ORDER = {
+    public static final int[] SETTINGS_ORDER = SiteSettingsHooksRegistry.getNewSettingsOrder(new int[] {
         ContentSettingsType.COOKIES,
         ContentSettingsType.GEOLOCATION,
         ContentSettingsType.GEOLOCATION_WITH_OPTIONS,
@@ -57,7 +57,7 @@ public class SiteSettingsUtil {
         ContentSettingsType.LOCAL_NETWORK_ACCESS,
         ContentSettingsType.LOCAL_NETWORK,
         ContentSettingsType.LOOPBACK_NETWORK,
-    };
+    });
 
     static final int[] CHOOSER_PERMISSIONS = {
         ContentSettingsType.USB_CHOOSER_DATA,
diff --git a/components/browser_ui/site_settings/android/java/src/org/chromium/components/browser_ui/site_settings/Website.java b/components/browser_ui/site_settings/android/java/src/org/chromium/components/browser_ui/site_settings/Website.java
index 6f59a2fb151fa..5ae70b46ce927 100644
--- a/components/browser_ui/site_settings/android/java/src/org/chromium/components/browser_ui/site_settings/Website.java
+++ b/components/browser_ui/site_settings/android/java/src/org/chromium/components/browser_ui/site_settings/Website.java
@@ -278,6 +278,22 @@ public final class Website implements WebsiteEntry {
     @VisibleForTesting
     @Nullable ContentSettingException createContentSettingException(
             @ContentSettingsType.EnumType int type, @ContentSetting int value) {
+        WebsitePermissionsFetcherHooks hooks =
+                SiteSettingsHooksRegistry.getWebsitePermissionsFetcherHooks(type);
+        if (!hooks.isDefaultInstance()) {
+            if (hooks.shouldCreateExceptionIfNull()) {
+                int permissionTypeOverride = hooks.getPermissionTypeOverride();
+                if (permissionTypeOverride == WebsitePermissionsFetcherHooks.CONTENT_SETTING_EXCEPTION) {
+                    return new ContentSettingException(
+                            type,
+                            assumeNonNull(resolvePrimaryPatternForException(getAddress())),
+                            value,
+                            ProviderType.NONE,
+                            /* isEmbargoed= */ false);
+                }
+            }
+        }
+
         if (type == ContentSettingsType.ADS) {
             // It is possible to set the permission without having an existing exception,
             // because we can show the BLOCK state even when this permission is set to the
@@ -318,6 +334,26 @@ public final class Website implements WebsiteEntry {
             @ContentSettingsType.EnumType int type,
             @ContentSetting int value) {
         PermissionInfo permissionInfo = getPermissionInfo(type);
+        WebsitePermissionsFetcherHooks hooks =
+                SiteSettingsHooksRegistry.getWebsitePermissionsFetcherHooks(type);
+        if (!hooks.isDefaultInstance()) {
+            if (hooks.shouldCreateExceptionIfNull()) {
+                int permissionTypeOverride = hooks.getPermissionTypeOverride();
+                if (permissionTypeOverride == WebsitePermissionsFetcherHooks.PERMISSION_INFO) {
+                    if (permissionInfo == null) {
+                        permissionInfo =
+                                new PermissionInfo(
+                                        type,
+                                        mOrigin.getOrigin(),
+                                        /* embedder= */ null,
+                                        /* isEmbargoed= */ false,
+                                        SessionModel.DURABLE);
+                        setPermissionInfo(permissionInfo);
+                    }
+                }
+            }
+        }
+
         if (type == ContentSettingsType.AUTO_PICTURE_IN_PICTURE) {
             // The Auto Picture-in-Picture permission is defaulted to allowed/denied based on the
             // incognito status. When the user explicitly sets the permission for the first time, no
diff --git a/components/browser_ui/site_settings/android/java/src/org/chromium/components/browser_ui/site_settings/WebsitePermissionsFetcher.java b/components/browser_ui/site_settings/android/java/src/org/chromium/components/browser_ui/site_settings/WebsitePermissionsFetcher.java
index baf718033f8bb..e9c628990fd09 100644
--- a/components/browser_ui/site_settings/android/java/src/org/chromium/components/browser_ui/site_settings/WebsitePermissionsFetcher.java
+++ b/components/browser_ui/site_settings/android/java/src/org/chromium/components/browser_ui/site_settings/WebsitePermissionsFetcher.java
@@ -77,6 +77,25 @@ public class WebsitePermissionsFetcher {
      */
     public static @Nullable WebsitePermissionsType getPermissionsType(
             @ContentSettingsType.EnumType int contentSettingsType) {
+        WebsitePermissionsFetcherHooks hooks =
+                SiteSettingsHooksRegistry.getWebsitePermissionsFetcherHooks(contentSettingsType);
+        if (!hooks.isDefaultInstance()) {
+            int permissionTypeOverride = hooks.getPermissionTypeOverride();
+            switch (permissionTypeOverride) {
+                case WebsitePermissionsFetcherHooks.CONTENT_SETTING_EXCEPTION -> {
+                    return WebsitePermissionsType.CONTENT_SETTING_EXCEPTION;
+                }
+                case WebsitePermissionsFetcherHooks.PERMISSION_INFO -> {
+                    return WebsitePermissionsType.PERMISSION_INFO;
+                }
+                case WebsitePermissionsFetcherHooks.CHOSEN_OBJECT_INFO -> {
+                    return WebsitePermissionsType.CHOSEN_OBJECT_INFO;
+                }
+                default -> {
+                }
+            }
+        }
+
         switch (contentSettingsType) {
             case ContentSettingsType.ADS:
             case ContentSettingsType.ANTI_ABUSE:
diff --git a/components/browser_ui/site_settings/android/java/src/org/chromium/components/browser_ui/site_settings/WebsitePermissionsFetcherHooks.java b/components/browser_ui/site_settings/android/java/src/org/chromium/components/browser_ui/site_settings/WebsitePermissionsFetcherHooks.java
new file mode 100644
index 0000000000000..1c5147680b2b8
--- /dev/null
+++ b/components/browser_ui/site_settings/android/java/src/org/chromium/components/browser_ui/site_settings/WebsitePermissionsFetcherHooks.java
@@ -0,0 +1,55 @@
+package org.chromium.components.browser_ui.site_settings;
+
+import androidx.annotation.IntDef;
+
+import org.chromium.build.annotations.AlwaysInline;
+import org.chromium.build.annotations.NonNull;
+import org.chromium.build.annotations.NullMarked;
+import org.chromium.build.annotations.Nullable;
+import org.chromium.components.content_settings.ContentSettingsType;
+
+@NullMarked
+public class WebsitePermissionsFetcherHooks {
+
+    private static final class LazyHolder {
+        @NonNull
+        private static final WebsitePermissionsFetcherHooks DEF_INSTANCE =
+                new WebsitePermissionsFetcherHooks();
+    }
+
+    protected WebsitePermissionsFetcherHooks() {
+    }
+
+    protected final boolean isDefaultInstance() {
+        return this == getDefaultInstance();
+    }
+
+    @NonNull
+    protected static final WebsitePermissionsFetcherHooks getDefaultInstance() {
+        return LazyHolder.DEF_INSTANCE;
+    }
+
+    static final int UNKNOWN = -1;
+    static final int CONTENT_SETTING_EXCEPTION = 0;
+    static final int PERMISSION_INFO = 1;
+    static final int CHOSEN_OBJECT_INFO = 2;
+
+    @IntDef({
+        UNKNOWN,
+        CONTENT_SETTING_EXCEPTION,
+        PERMISSION_INFO,
+        CHOSEN_OBJECT_INFO,
+    })
+    @interface PermissionTypeOverride {
+    }
+
+    @PermissionTypeOverride
+    protected int getPermissionTypeOverride() {
+        return UNKNOWN;
+    }
+
+    protected boolean shouldCreateExceptionIfNull() {
+        return false;
+    }
+}
+
diff --git a/components/browser_ui/site_settings/android/site_settings_android_java_ext_sources.gni b/components/browser_ui/site_settings/android/site_settings_android_java_ext_sources.gni
index cefd08e1de29c..16f888c2b83ed 100644
--- a/components/browser_ui/site_settings/android/site_settings_android_java_ext_sources.gni
+++ b/components/browser_ui/site_settings/android/site_settings_android_java_ext_sources.gni
@@ -3,4 +3,11 @@
 # found in the LICENSE file.
 
 site_settings_android_java_ext_sources = [
+  "java/src/org/chromium/components/browser_ui/site_settings/SingleCategorySettingsHooks.java",
+  "java/src/org/chromium/components/browser_ui/site_settings/SingleWebsiteSettingsHooks.java",
+  "java/src/org/chromium/components/browser_ui/site_settings/SiteSettingsCategoryHooks.java",
+  "java/src/org/chromium/components/browser_ui/site_settings/SiteSettingsHooksRegistry.java",
+  "java/src/org/chromium/components/browser_ui/site_settings/SiteSettingsResourcesHooks.java",
+  "java/src/org/chromium/components/browser_ui/site_settings/SiteSettingsScreenHooks.java",
+  "java/src/org/chromium/components/browser_ui/site_settings/WebsitePermissionsFetcherHooks.java",
 ]
