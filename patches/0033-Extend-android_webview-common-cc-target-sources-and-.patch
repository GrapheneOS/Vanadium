From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: quh4gko8 <88831734+quh4gko8@users.noreply.github.com>
Date: Mon, 13 Mar 2023 08:41:43 +0000
Subject: [PATCH] Extend //android_webview/common cc target sources and deps

---
 android_webview/common/BUILD.gn               |  5 ++++
 .../common/common_cc_ext_deps.gni             | 26 +++++++++++++++++++
 .../common/common_cc_ext_sources.gni          | 26 +++++++++++++++++++
 3 files changed, 57 insertions(+)
 create mode 100644 vanadium/chromium_src/android_webview/common/common_cc_ext_deps.gni
 create mode 100644 vanadium/chromium_src/android_webview/common/common_cc_ext_sources.gni

diff --git a/android_webview/common/BUILD.gn b/android_webview/common/BUILD.gn
index e4f6810ca7b08..6a787b6a05f6f 100644
--- a/android_webview/common/BUILD.gn
+++ b/android_webview/common/BUILD.gn
@@ -66,6 +66,11 @@ source_set("common") {
   ]
 
   public_deps = [ "//url" ]
+
+ import("//vanadium/chromium_src/android_webview/common/common_cc_ext_sources.gni")
+ sources += common_cc_ext_sources
+ import("//vanadium/chromium_src/android_webview/common/common_cc_ext_deps.gni")
+ deps += common_cc_ext_deps
 }
 
 mojom("mojom") {
diff --git a/vanadium/chromium_src/android_webview/common/common_cc_ext_deps.gni b/vanadium/chromium_src/android_webview/common/common_cc_ext_deps.gni
new file mode 100644
index 0000000000000..cfc4f6a3910a3
--- /dev/null
+++ b/vanadium/chromium_src/android_webview/common/common_cc_ext_deps.gni
@@ -0,0 +1,26 @@
+# Copyright 2023-2026 GrapheneOS
+# Use of this source code is governed by a GPL-2.0-only style license that can be
+# found in the LICENSE file.
+
+# Three lines of code for minimizing conflicts in rebasing changes
+# Three lines of code for minimizing conflicts in rebasing changes
+# Three lines of code for minimizing conflicts in rebasing changes
+common_cc_ext_full_path_deps = [
+]
+# Three lines of code for minimizing conflicts in rebasing changes
+# Three lines of code for minimizing conflicts in rebasing changes
+# Three lines of code for minimizing conflicts in rebasing changes
+common_cc_ext_rel_path_deps = [
+]
+# Three lines of code for minimizing conflicts in rebasing changes
+# Three lines of code for minimizing conflicts in rebasing changes
+# Three lines of code for minimizing conflicts in rebasing changes
+common_cc_ext_deps = [
+]
+# Three lines of code for minimizing conflicts in rebasing changes
+# Three lines of code for minimizing conflicts in rebasing changes
+# Three lines of code for minimizing conflicts in rebasing changes
+common_cc_ext_deps += common_cc_ext_full_path_deps
+foreach(rel_path, common_cc_ext_rel_path_deps) {
+  common_cc_ext_deps += [ rebase_path(rel_path) ]
+}
diff --git a/vanadium/chromium_src/android_webview/common/common_cc_ext_sources.gni b/vanadium/chromium_src/android_webview/common/common_cc_ext_sources.gni
new file mode 100644
index 0000000000000..04a7e50080265
--- /dev/null
+++ b/vanadium/chromium_src/android_webview/common/common_cc_ext_sources.gni
@@ -0,0 +1,26 @@
+# Copyright 2023-2026 GrapheneOS
+# Use of this source code is governed by a GPL-2.0-only style license that can be
+# found in the LICENSE file.
+
+# Three lines of code for minimizing conflicts in rebasing changes
+# Three lines of code for minimizing conflicts in rebasing changes
+# Three lines of code for minimizing conflicts in rebasing changes
+common_cc_ext_full_path_sources = [
+]
+# Three lines of code for minimizing conflicts in rebasing changes
+# Three lines of code for minimizing conflicts in rebasing changes
+# Three lines of code for minimizing conflicts in rebasing changes
+common_cc_ext_rel_path_sources = [
+]
+# Three lines of code for minimizing conflicts in rebasing changes
+# Three lines of code for minimizing conflicts in rebasing changes
+# Three lines of code for minimizing conflicts in rebasing changes
+common_cc_ext_sources = [
+]
+# Three lines of code for minimizing conflicts in rebasing changes
+# Three lines of code for minimizing conflicts in rebasing changes
+# Three lines of code for minimizing conflicts in rebasing changes
+common_cc_ext_sources += common_cc_ext_full_path_sources
+foreach(rel_path, common_cc_ext_rel_path_sources) {
+  common_cc_ext_sources += [ rebase_path(rel_path) ]
+}
