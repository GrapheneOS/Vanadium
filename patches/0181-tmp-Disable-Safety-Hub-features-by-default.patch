From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: quh4gko8 <88831734+quh4gko8@users.noreply.github.com>
Date: Sat, 30 Nov 2024 07:07:47 +0000
Subject: [PATCH] tmp: Disable Safety Hub features by default

---
 chrome/common/chrome_features.cc    | 8 ++++----
 components/safety_check/features.cc | 2 +-
 2 files changed, 5 insertions(+), 5 deletions(-)

diff --git a/chrome/common/chrome_features.cc b/chrome/common/chrome_features.cc
index 558f27f8bc440..b7e95d6ecb311 100644
--- a/chrome/common/chrome_features.cc
+++ b/chrome/common/chrome_features.cc
@@ -1101,7 +1101,7 @@ BASE_FEATURE(RecordWebAppDebugInfo, base::FEATURE_DISABLED_BY_DEFAULT);
 // Enables notification permission revocation for abusive origins.
 BASE_FEATURE(kAbusiveNotificationPermissionRevocation,
              "AbusiveOriginNotificationPermissionRevocation",
-             base::FEATURE_ENABLED_BY_DEFAULT);
+             base::FEATURE_DISABLED_BY_DEFAULT);
 
 #if BUILDFLAG(IS_CHROMEOS)
 // Enables permanent removal of Legacy Supervised Users on startup.
@@ -1120,7 +1120,7 @@ BASE_FEATURE(SafetyHubExtensionsOffStoreTrigger,
              base::FEATURE_ENABLED_BY_DEFAULT);
 #endif
 
-BASE_FEATURE(SafetyHubThreeDotDetails, base::FEATURE_ENABLED_BY_DEFAULT);
+BASE_FEATURE(SafetyHubThreeDotDetails, base::FEATURE_DISABLED_BY_DEFAULT);
 
 BASE_FEATURE(kSafetyHubUnusedPermissionRevocationForAllSurfaces,
              "SafetyHubUnusedPermissionRevocationForAllSurfaces",
@@ -1191,10 +1191,10 @@ constexpr base::FeatureParam<int>
 
 #if BUILDFLAG(IS_ANDROID)
 // Enables Safety Hub card in magic stack.
-BASE_FEATURE(SafetyHubMagicStack, base::FEATURE_ENABLED_BY_DEFAULT);
+BASE_FEATURE(SafetyHubMagicStack, base::FEATURE_DISABLED_BY_DEFAULT);
 
 // Enables Safety Hub followup work.
-BASE_FEATURE(SafetyHubFollowup, base::FEATURE_ENABLED_BY_DEFAULT);
+BASE_FEATURE(SafetyHubFollowup, base::FEATURE_DISABLED_BY_DEFAULT);
 
 // Enables Safety Hub organic HaTS survey on Android.
 BASE_FEATURE(SafetyHubAndroidOrganicSurvey, base::FEATURE_DISABLED_BY_DEFAULT);
diff --git a/components/safety_check/features.cc b/components/safety_check/features.cc
index 1f3288254237f..542ae9e227bfa 100644
--- a/components/safety_check/features.cc
+++ b/components/safety_check/features.cc
@@ -8,7 +8,7 @@
 
 namespace safety_check::features {
 
-BASE_FEATURE(SafetyHub, base::FEATURE_ENABLED_BY_DEFAULT);
+BASE_FEATURE(SafetyHub, base::FEATURE_DISABLED_BY_DEFAULT);
 
 // Time between automated runs of the password check.
 const base::FeatureParam<base::TimeDelta> kBackgroundPasswordCheckInterval{
