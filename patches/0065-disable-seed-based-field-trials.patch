From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: quh4gko8 <88831734+quh4gko8@users.noreply.github.com>
Date: Thu, 4 Jun 2026 03:30:18 +0000
Subject: [PATCH] disable seed-based field trials

---
 .../service/variations_field_trial_creator.cc     | 15 +++++++++++++++
 components/variations/synthetic_trial_registry.cc |  3 +++
 .../variations/service/services_cc_ext_deps.gni   |  1 +
 .../variations/variations_cc_ext_deps.gni         |  1 +
 4 files changed, 20 insertions(+)

diff --git a/components/variations/service/variations_field_trial_creator.cc b/components/variations/service/variations_field_trial_creator.cc
index e044db184174e..56e7c5f8d6772 100644
--- a/components/variations/service/variations_field_trial_creator.cc
+++ b/components/variations/service/variations_field_trial_creator.cc
@@ -59,6 +59,7 @@
 #include "components/variations/variations_switches.h"
 #include "components/version_info/version_info.h"
 #include "ui/base/l10n/l10n_util.h"
+#include "vanadium/chromium_src/components/variations/common_ext/buildflags.h"
 
 namespace variations {
 namespace {
@@ -267,9 +268,15 @@ bool VariationsFieldTrialCreator::SetUpFieldTrials(
 
   // Force the variation ids selected in chrome://flags and/or specified using
   // the command-line flag.
+#if BUILDFLAG(FIELDTRIAL_SEED_ENABLED)
   auto result = http_header_provider->ForceVariationIds(
       base::PassKey<VariationsFieldTrialCreator>(), variation_ids,
       command_line->GetSwitchValueASCII(switches::kForceVariationIds));
+#else
+  // Pretend that it was successful without acutally forcing
+  // variation ids and command line variation ids
+  auto result = VariationsIdsProvider::ForceIdsResult::SUCCESS;
+#endif // BUILDFLAG(FIELDTRIAL_SEED_ENABLED)
 
   switch (result) {
     case VariationsIdsProvider::ForceIdsResult::INVALID_SWITCH_ENTRY:
@@ -347,27 +354,35 @@ bool VariationsFieldTrialCreator::SetUpFieldTrials(
 
   CreateTrialsResult create_trials_result = {.applied_seed = false};
   if (!used_testing_config && client_filterable_state) {
+#if BUILDFLAG(FIELDTRIAL_SEED_ENABLED)
     create_trials_result = CreateTrialsFromSeed(
         entropy_providers, feature_list.get(), safe_seed_manager,
         std::move(client_filterable_state));
+#endif // BUILDFLAG(FIELDTRIAL_SEED_ENABLED)
   }
 
   if (create_trials_result.applied_seed) {
+#if BUILDFLAG(FIELDTRIAL_SEED_ENABLED)
     FieldTrialsProvider::UpdateAppliedSeedHasActiveLimitedLayer(
         create_trials_result.seed_has_active_limited_layer.value_or(false));
     variations_source_.type = VariationsSourceType::kVariationsServer;
+#endif // BUILDFLAG(FIELDTRIAL_SEED_ENABLED)
   }
 
   if (add_entropy_source_to_variations_ids &&
       !create_trials_result.AppliedSeedHasActiveLimitedLayer()) {
+#if BUILDFLAG(FIELDTRIAL_SEED_ENABLED)
     // TODO(crbug.com/424154785): Consider no longer transmitting LES values
     // alongside VariationsIDs.
     http_header_provider->SetLowEntropySourceValue(
         metrics_state_manager->GetLowEntropySource());
+#endif // BUILDFLAG(FIELDTRIAL_SEED_ENABLED)
   }
 
+#if BUILDFLAG(FIELDTRIAL_SEED_ENABLED)
   platform_field_trials->SetUpClientSideFieldTrials(
       create_trials_result.applied_seed, entropy_providers, feature_list.get());
+#endif // BUILDFLAG(FIELDTRIAL_SEED_ENABLED)
 
   platform_field_trials->RegisterFeatureOverrides(feature_list.get());
 
diff --git a/components/variations/synthetic_trial_registry.cc b/components/variations/synthetic_trial_registry.cc
index 70263fba63798..cc663db0d070f 100644
--- a/components/variations/synthetic_trial_registry.cc
+++ b/components/variations/synthetic_trial_registry.cc
@@ -13,6 +13,7 @@
 #include "components/variations/active_field_trials.h"
 #include "components/variations/hashing.h"
 #include "components/variations/variations_associated_data.h"
+#include "vanadium/chromium_src/components/variations/common_ext/buildflags.h"
 
 namespace variations {
 namespace internal {
@@ -143,6 +144,7 @@ void SyntheticTrialRegistry::RegisterExternalExperimentsInternal(
 
 void SyntheticTrialRegistry::RegisterSyntheticFieldTrial(
     const SyntheticTrialGroup& trial) {
+#if BUILDFLAG(FIELDTRIAL_SEED_ENABLED)
   for (auto& entry : synthetic_trial_groups_) {
     if (entry.id().name == trial.id().name) {
       if (entry.id().group != trial.id().group ||
@@ -160,6 +162,7 @@ void SyntheticTrialRegistry::RegisterSyntheticFieldTrial(
   trial_group.SetStartTime(base::TimeTicks::Now());
   synthetic_trial_groups_.push_back(trial_group);
   NotifySyntheticTrialObservers({trial_group}, {});
+#endif // BUILDFLAG(FIELDTRIAL_SEED_ENABLED)
 }
 
 SyntheticTrialRegistry::ExternalExperiment
diff --git a/vanadium/chromium_src/components/variations/service/services_cc_ext_deps.gni b/vanadium/chromium_src/components/variations/service/services_cc_ext_deps.gni
index 2c9b13a4b3b03..89b2b4fd3deeb 100644
--- a/vanadium/chromium_src/components/variations/service/services_cc_ext_deps.gni
+++ b/vanadium/chromium_src/components/variations/service/services_cc_ext_deps.gni
@@ -6,6 +6,7 @@
 # Three lines of code for minimizing conflicts in rebasing changes
 # Three lines of code for minimizing conflicts in rebasing changes
 services_cc_ext_full_path_deps = [
+  "//vanadium/chromium_src/components/variations/common_ext:buildflags",
 ]
 # Three lines of code for minimizing conflicts in rebasing changes
 # Three lines of code for minimizing conflicts in rebasing changes
diff --git a/vanadium/chromium_src/components/variations/variations_cc_ext_deps.gni b/vanadium/chromium_src/components/variations/variations_cc_ext_deps.gni
index 4a8d9f1dc8e76..8023e5169d4dc 100644
--- a/vanadium/chromium_src/components/variations/variations_cc_ext_deps.gni
+++ b/vanadium/chromium_src/components/variations/variations_cc_ext_deps.gni
@@ -6,6 +6,7 @@
 # Three lines of code for minimizing conflicts in rebasing changes
 # Three lines of code for minimizing conflicts in rebasing changes
 variations_cc_ext_full_path_deps = [
+  "//vanadium/chromium_src/components/variations/common_ext:buildflags",
 ]
 # Three lines of code for minimizing conflicts in rebasing changes
 # Three lines of code for minimizing conflicts in rebasing changes
