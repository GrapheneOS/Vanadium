From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: quh4gko8 <88831734+quh4gko8@users.noreply.github.com>
Date: Mon, 13 Mar 2023 08:41:43 +0000
Subject: [PATCH] Extend //net/dns cc target sources and deps

---
 net/dns/BUILD.gn                              |  5 ++++
 .../chromium_src/net/dns/dns_cc_ext_deps.gni  | 26 +++++++++++++++++++
 .../net/dns/dns_cc_ext_sources.gni            | 26 +++++++++++++++++++
 3 files changed, 57 insertions(+)
 create mode 100644 vanadium/chromium_src/net/dns/dns_cc_ext_deps.gni
 create mode 100644 vanadium/chromium_src/net/dns/dns_cc_ext_sources.gni

diff --git a/net/dns/BUILD.gn b/net/dns/BUILD.gn
index d97fb126e1bb2..53c8aaff31243 100644
--- a/net/dns/BUILD.gn
+++ b/net/dns/BUILD.gn
@@ -220,6 +220,11 @@ source_set("dns") {
     ":host_resolver_manager",
     ":mdns_client",
   ]
+
+  import("//vanadium/chromium_src/net/dns/dns_cc_ext_sources.gni")
+  sources += dns_cc_ext_sources
+  import("//vanadium/chromium_src/net/dns/dns_cc_ext_deps.gni")
+  deps += dns_cc_ext_deps
 }
 
 # The standard API of net/dns.
diff --git a/vanadium/chromium_src/net/dns/dns_cc_ext_deps.gni b/vanadium/chromium_src/net/dns/dns_cc_ext_deps.gni
new file mode 100644
index 0000000000000..31dbebad10fac
--- /dev/null
+++ b/vanadium/chromium_src/net/dns/dns_cc_ext_deps.gni
@@ -0,0 +1,26 @@
+# Copyright 2023-2026 GrapheneOS
+# Use of this source code is governed by a GPL-2.0-only style license that can be
+# found in the LICENSE file.
+
+# Three lines of code for minimizing conflicts in rebasing changes
+# Three lines of code for minimizing conflicts in rebasing changes
+# Three lines of code for minimizing conflicts in rebasing changes
+dns_cc_ext_full_path_deps = [
+]
+# Three lines of code for minimizing conflicts in rebasing changes
+# Three lines of code for minimizing conflicts in rebasing changes
+# Three lines of code for minimizing conflicts in rebasing changes
+dns_cc_ext_rel_path_deps = [
+]
+# Three lines of code for minimizing conflicts in rebasing changes
+# Three lines of code for minimizing conflicts in rebasing changes
+# Three lines of code for minimizing conflicts in rebasing changes
+dns_cc_ext_deps = [
+]
+# Three lines of code for minimizing conflicts in rebasing changes
+# Three lines of code for minimizing conflicts in rebasing changes
+# Three lines of code for minimizing conflicts in rebasing changes
+dns_cc_ext_deps += dns_cc_ext_full_path_deps
+foreach(rel_path, dns_cc_ext_rel_path_deps) {
+  dns_cc_ext_deps += [ rebase_path(rel_path) ]
+}
diff --git a/vanadium/chromium_src/net/dns/dns_cc_ext_sources.gni b/vanadium/chromium_src/net/dns/dns_cc_ext_sources.gni
new file mode 100644
index 0000000000000..755dcc76525ee
--- /dev/null
+++ b/vanadium/chromium_src/net/dns/dns_cc_ext_sources.gni
@@ -0,0 +1,26 @@
+# Copyright 2023-2026 GrapheneOS
+# Use of this source code is governed by a GPL-2.0-only style license that can be
+# found in the LICENSE file.
+
+# Three lines of code for minimizing conflicts in rebasing changes
+# Three lines of code for minimizing conflicts in rebasing changes
+# Three lines of code for minimizing conflicts in rebasing changes
+dns_cc_ext_full_path_sources = [
+]
+# Three lines of code for minimizing conflicts in rebasing changes
+# Three lines of code for minimizing conflicts in rebasing changes
+# Three lines of code for minimizing conflicts in rebasing changes
+dns_cc_ext_rel_path_sources = [
+]
+# Three lines of code for minimizing conflicts in rebasing changes
+# Three lines of code for minimizing conflicts in rebasing changes
+# Three lines of code for minimizing conflicts in rebasing changes
+dns_cc_ext_sources = [
+]
+# Three lines of code for minimizing conflicts in rebasing changes
+# Three lines of code for minimizing conflicts in rebasing changes
+# Three lines of code for minimizing conflicts in rebasing changes
+dns_cc_ext_sources += dns_cc_ext_full_path_sources
+foreach(rel_path, dns_cc_ext_rel_path_sources) {
+  dns_cc_ext_sources += [ rebase_path(rel_path) ]
+}
